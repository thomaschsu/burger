<div class="text-center jumbotron">
    <h1 class="display-4">Eat-Dat-Burger!</h1>
    <p class="lead">A burger app that uses MySQL, Node, Handlebars.js, Express, ORM, JavaScript, HTML, and CSS to let you add and devour a burger of your choice. Input any burger of your choice.</p>
</div>
<div class="text-center">
   <img src="assets/img/burger.gif">
</div>
<div class="container">
   <div class="row">
      <div class="col-s width-fix">
         <h3>Available Burgers</h3>
         <ul>
            {{#each burgers}}
            {{#unless devoured}}
            <li>
               {{id}} - {{burger_name}}
               <button class="devour-burger" data-id="{{id}}">
               DEVOUR!
               </button>
            </li>
            {{/unless}}
            {{/each}}
         </ul>
      </div>
      <div class="col-s width-fix-2">
         <h3 style="color:#333">Devoured Burgers</h3>
         <ul>
            {{#each burgers}}
            {{#if devoured}}
            <li>
               {{id}} - {{burger_name}}
            </li>
            {{/if}}
            {{/each}}
         </ul>
      </div>
   </div>
</div>
<div class="container text-center">
<h3 style="color: #FFF">Add a Burger</h3>
<form class="create-form">
   <div class="form-group">
      <textarea rows="4" cols="50" name="name" id="ca" placeholder="Type a Burger Name Here..."></textarea>
   </div>
   <button type="submit">Submit</button>
</form>
</div>